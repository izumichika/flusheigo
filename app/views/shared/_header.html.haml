.header
  %h.header__title
    MY単語
  .header__right
    - if user_signed_in?
      .header__right__link
        =link_to "ログアウト", destroy_user_session_path, method: :delete 
    - else
      .header__right__link
        = link_to "ログイン", new_session_path(resource_name)
